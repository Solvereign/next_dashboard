import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
	/* config options here */
	// experimental: {  // hamgiin suullin nextjs huwilbar deer l boldog gene ee
	// 	ppr: 'incremental'
	// }
};

export default nextConfig;
